[global]
type = global
user_agent = ${ASTERISK_USER_AGENT}

[transport-udp]
type = transport
protocol = udp
bind = 0.0.0.0
;local_net = LOCAL_NET
external_media_address = ${PUBLIC_IP_ADDRESS}
external_signaling_address = ${PUBLIC_IP_ADDRESS}
allow_reload = yes
